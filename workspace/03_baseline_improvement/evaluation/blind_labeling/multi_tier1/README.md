# S2 Multi Tier-1 预注册盲标包

本集合在查看新标定结果前冻结，共 15 题，覆盖五个领域。

## 选择构成

- 6 道既定全选题：`fc_a_016`、`fin_a_005`、`ins_a_005`、`ins_a_010`、`ins_a_017`、`res_a_014`；
- v1s1 改变的 8 道保险 Multi：`ins_a_005`、`ins_a_007`、`ins_a_009`、`ins_a_010`、`ins_a_014`、`ins_a_015`、`ins_a_016`、`ins_a_019`；
- 两组并集为 12 题，再补 3 道非保险三选题：`fc_a_001`、`fin_a_002`、`reg_a_002`。

## 完成要求

- 每个选项分别核对主体、时间、数值、单位、比较方向、范围、条件和否定词；
- 只有全部关键要素有证据才判定该选项成立；
- 每题记录完整选项组合、置信度和 1–2 条决定性证据；
- 不查看 v0/v1/v2 答案，全部交回后再做新旧答案对照；
- 进入 S3 的样本需升级为 Tier-2，补齐所有必要事实和 chunk 指针。

预注册集合的机器可读版本为 `selection.json`。

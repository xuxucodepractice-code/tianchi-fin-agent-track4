# E007R1 Decision

状态：`PREREGISTERED / DEVELOPMENT_NOT_RUN / DO_NOT_SUBMIT`

- E007 原失败目录只读保留；
- 无 key TLS 探针通过，CA bundle 待纳入 code/run freeze；
- control/treatment 新输出槽、authorization 与 claim 均须为空；
- prospective、candidate、answer.csv、上传、push 和 main merge 均未授权。

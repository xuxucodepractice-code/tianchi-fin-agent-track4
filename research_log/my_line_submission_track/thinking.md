# thinking.md — 提交线跑测思考记录

> 用途：记录每一版跑测的**分数、哪类题错、我猜为什么错**。
> 这份记录是我反哺学习线的东西，也是我"真的参与了、没被 AI 替我思考"的证据。
> 配套文件：`taacClaude.md`（工作手册）。
> 规则：每写一段代码前先让 AI 讲清设计→我听懂→再落地；代码 AI 写，跑/看结果/判断下一步是我的。

---

## 记录模板（每一版复制一份）

### 版本 vX —— 日期 ____
- **这版改了什么 / 是什么系统：**
- **对应 commit（如有）：**
- **数据范围：**（1题 / 5题 / 100题）
- **命令：**
- **是否成功生成 answer.csv：**
- **是否成功上传：**
- **线上分数（FinalScore）：**
- **准确率（如能估）：**
- **Token 消耗：**
- **分领域对错情况：** insurance __/20  regulatory __/20  contracts __/20  reports __/20  research __/20
- **错得最多的题型 / 领域：**
- **我猜为什么错（我的判断，不是 AI 给的）：**
- **下一步该改哪（我的决定）：**
- **该回到学习线深挖的问题：**

---

## 待办里程碑（勾掉表示做到了）

- [ ] 第 0 步：拿到 Qwen API key，能调 Qwen3.6-plus
- [ ] 第 1 步：建好 submissions/first_submission/notes.md
- [ ] 第 2 步：调通一次 Qwen 并打印出 token 用量
- [ ] 第 3 步：跑通 1 题（ins_a_007，对照人肉答案 BC）
- [ ] 第 4 步：跑完 100 题，生成合法 answer.csv
- [ ] 第 5 步：首次提交，拿到第一个线上分数 ← 核心 KPI（保 A 榜资格）

---

## 跑测日志（在下面按版本往下记）

<!-- 从这里开始，每跑一版就在下面新增一段，用上面的模板 -->

### 版本 v1 —— 日期 2026-07-04
- **这版改了什么 / 是什么系统：** 提交线第一个最小 workflow（固定流程，非自由 agent）：读题→doc_id映射→pypdf抽文字→盲截断前3000字→拼prompt→调Qwen→取答案。脚本 `submissions/step3_solve_one.py`。
- **对应 commit（如有）：** 未提交
- **数据范围：** 1 题（ins_a_007）
- **命令：** `.venv/bin/python step3_solve_one.py`
- **是否成功生成 answer.csv：** 否（第3步只跑通链路，还没到出csv）
- **是否成功上传：** 否
- **线上分数（FinalScore）：** 无（本地测）
- **准确率（如能估）：** 0/1
- **Token 消耗：** 4402（输入4401 输出1）
- **分领域对错情况：** insurance 0/1（只跑了1题）
- **错得最多的题型 / 领域：** 本题多选题(multi)
- **我猜为什么错（我的判断，不是 AI 给的）：** Qwen 答 B，标尺是 BC，少答 C。盲截断只取每份文档前3000字，而"保单贷款"条款一般在保险条款中后部；C 选项(平安富鸿金生)的关键证据很可能被截断截掉了，Qwen 没看到依据所以不敢选。
- **下一步该改哪（我的决定）：** 不能只取文档开头。要能定位到含"保单贷款"关键词的段落再喂给 Qwen。
- **该回到学习线深挖的问题：** 在"禁止 embedding"约束下，怎么用关键词/BM25/规则检索，准确找到正确证据段落？（对应学习线阶段 5/6）

---

### 版本 v2 + v3 —— 日期 2026-07-04（同一天两跑，合并记）
- **这版改了什么 / 是什么系统：** 从"跑1题"扩到"跑100题出answer.csv"。v2 用仓库旧数据跑，v3 换成官方zip正确数据重跑。
- **对应 commit（如有）：** 未提交
- **数据范围：** 100 题（全部A榜）
- **命令：** `.venv/bin/python step4_run_all.py`
- **是否成功生成 answer.csv：** 是（v3 数据地基正确）
- **是否成功上传：** 否（还没交）
- **线上分数（FinalScore）：** 无（本地无标答，算不出准确率，只能靠提交拿真分）
- **准确率（如能估）：** 本地无法精确算（官方数据里不含标准答案，我核实过 zip 里没有 answer 文件）
- **Token 消耗：** v3 = 423605，TokenScore≈0.92
- **分领域对错情况：** 无标答，填不了对错；但从"文档有没有读进去"看：v2 时 fc/fin/reg 共60题文档没读对，v3 全部读对了
- **错得最多的题型 / 领域：** 待提交后才知道；预判 fc/fin（年报大文档，盲截断只读到封面目录）最差
- **我猜为什么错（我的判断，不是 AI 给的）：**
  1. 【最大发现】之前用错了数据版本——仓库里的 public_dataset_upload 文件命名和题目 doc_ids 对不上，导致60题读空或读错文档。换成官方zip数据后修复。
  2. 盲截断只取每份文档前3000字，对年报/研报这种几百页的大文档，前3000字基本是封面目录，关键数据在后面读不到。
- **下一步该改哪（我的决定）：** （待定，先提交拿基线分，还是先改进检索）
- **该回到学习线深挖的问题：** 大文档怎么在"禁embedding"下定位到关键段落？按关键词？按章节标题？按条款号？

---

